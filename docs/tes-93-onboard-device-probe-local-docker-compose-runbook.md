# Runbook: Onboard Device-Probe Local Docker-Compose Environment

## Introduction
This runbook provides steps to onboard the Device-Probe local docker-compose environment.

## Prerequisites
* Device-Probe local docker-compose setup
* Vault IAM role ARN and Consul agent health-check port noted

## Steps
1. Read Device-Probe local docker-compose
2. Note Vault IAM role ARN and Consul agent health-check port
3. Study internal Confluence page on Hazelcast map names used for per-batch probe aggregation state