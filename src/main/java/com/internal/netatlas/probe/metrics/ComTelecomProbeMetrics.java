package com.internal.netatlas.probe.metrics;\n\nimport io.micrometer.core.instrument.Counter;\nimport io.micrometer.core.instrument.Gauge;\nimport io.micrometer.core.instrument.MeterRegistry;\nimport org.springframework.stereotype.Component;\nimport software.amazon.awssdk.services.sqs.SqsClient;\nimport software.amazon.awssdk.services.sqs.model.GetQueueAttributesRequest;\nimport software.amazon.awssdk.services.sqs.model.QueueAttributeName;\nimport java.util.concurrent.atomic.AtomicInteger;\n\n/**\n * Micrometer metrics for the Device-Probe service.\n *\n * <p>Provides per‑protocol failure counters and a gauge exposing the current size of the DLQ\n * queue {@code platform.results.dlq}. The gauge is refreshed on demand by the health endpoint.\n */\n@Component\npublic class ComTelecomProbeMetrics {\n\n    private final Counter netconfFailureCounter;\n    private final Counter sshFailureCounter;\n    private final Counter snmpFailureCounter;\n    private final Counter eapiFailureCounter;\n    private final Counter grpcFailureCounter;\n\n    private final AtomicInteger dlqSizeGauge = new AtomicInteger(0);\n    private final SqsClient sqsClient;\n    private final String dlqUrl;\n    private final MeterRegistry registry;\n\n    public ComTelecomProbeMetrics(MeterRegistry registry, SqsClient sqsClient) {\n        this.registry = registry;\n        this.sqsClient = sqsClient;\n        this.dlqUrl = System.getenv(\"DLQ_QUEUE_URL\